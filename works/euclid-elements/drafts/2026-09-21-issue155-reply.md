Thanks for the fast merge, and for the context — the fidelity rule and the footnote convention are exactly what I was hoping to hear. I've since read through the lektor repo (`process.md`, the deck tracker, `check-decks.js`) so I understand the authoring side and not just the library.

Three concrete things I'd like to pick up, in whatever order suits you:

1. **File the XI.11 plane-rotation behaviour** you described above as its own issue so it's trackable, and take a run at it if you'd like.
2. **Spend time with the Book I–II slideshows** and send you presentation feedback in a separate issue, since you asked.
3. **Take Book IV's 16 decks** through `process.md`, submitting each planning table for your review before implementing — so you can see whether a second author keeps to your standard before it costs you anything.

One question about how I'd write those decks, since it affects the shape of the PRs: I'd like to author them as plain data — string animation names (which I see the registry already accepts), no `var` + `.concat()` for the visible sets, nothing but JSON-parseable literals inside `slides[]`. Nothing changes for existing pages or for `check-decks.js`; it just means the decks can be checked against a schema before the sandbox step, and later exported to the tools you mentioned (GeoGebra, Manim) without re-parsing JavaScript. If you'd rather I match the existing decks exactly, I'll do that instead — your call.

I've written the reasoning up as two short decision records, one on why I'm contributing rather than building and one on the authoring idea above, here: https://github.com/imkarrer/preservation-workbench/tree/main/docs/adr. The second is marked *proposed* and stays that way until you've had a look.

The Apollonius / Hilbert idea is the kind of thing I'd want to be part of.
